generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Deliveryman {
  id       String @id @unique @default(uuid())
  username String @unique
  password String

  @@map("deliveryman")
}
